export default function LibraryPage(): JSX.Element {
  return (
    <main className="container" style={{ padding: '32px 0' }}>
      <div className="panel">
        <h1 style={{ fontFamily: 'Cinzel, serif' }}>Case Library</h1>
        <p>Filter by subgenre, session length, and party mode.</p>
      </div>
    </main>
  );
}

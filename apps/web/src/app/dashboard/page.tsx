export default function DashboardPage(): JSX.Element {
  return (
    <main className="container" style={{ padding: '32px 0' }}>
      <div className="panel">
        <h1 style={{ fontFamily: 'Cinzel, serif' }}>Dashboard</h1>
        <p>Saved sessions, subscriptions, referrals, and progress history.</p>
      </div>
    </main>
  );
}
